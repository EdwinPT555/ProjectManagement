import { PrimaryButton } from '@fluentui/react'
import { useHistory } from 'react-router'

const ClientRequestsPage = () => {
    const history = useHistory();

    return (
        <div>
            <span>---------------...........</span>
            <h1>client Requests page</h1>

        </div>
    )
}

export default ClientRequestsPage
